---
title: setting up graphql file uploads
date: 2019-12-01
---

- apollo-upload-client, uppy, formik on frontend
- apollo-server-express, webpack, body-parser, prisma on backend
- setup apollo server with hot reloading NB i import the schema directly to make the server faster
- add scalar to schema.graphql & replace it with empty string in schema.ts (fix)
- setup frontend & use upload link instead of http link
- uppy component with custom on complete method
- add fileId to formik values when done
- submit mutation
Hello, world! This is a demo post for `gatsby-theme-blog`.

Delete me, and get writing!

```js:title=gatsby-config.js
module.exports = {
  plugins: [
    "gatsby-theme-blog", // highlight-line
    "gatsby-theme-notes"
  ]
};
```

This is another paragraph after the code block.

## This is a secondary heading

```jsx
import React from "react";
import { ThemeProvider } from "theme-ui";
import theme from "./theme";

export default props => (
  <ThemeProvider theme={theme}>{props.children}</ThemeProvider>
);
```
